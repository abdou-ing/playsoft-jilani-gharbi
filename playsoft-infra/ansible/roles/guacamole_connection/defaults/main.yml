---
# Guacamole admin credentials
auth_username: ""
auth_password: ""

# Path to root Terraform output JSON (edge_public_ip)
tf_output_path_root: "~/playsoft-jilani-gharbi/playsoft-infra/terraform/tf_output.json"

# Path to Proxmox Terraform output JSON (vm_ids)
tf_output_path_proxmox: "~/playsoft-jilani-gharbi/playsoft-infra/terraform/modules/vnc-server/tf_output.json"

# Template config — auto-generates one user per VM
user_prefix: "candidat"
user_password_prefix: "candidat"
user_organization: "playsoft"
user_email_domain: "example.com"
connection_prefix: "candidat-exam"
vnc_hostname: "138.201.200.168"
vnc_base_port: 5901   

# Populated at runtime from vm_ids Terraform output
guacamole_users: []

