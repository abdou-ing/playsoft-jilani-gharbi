# AUTO-GENERATED by roles/autoscaler -- do not edit manually. Re-templated
# from tf_output.json on every deploy (see roles/autoscaler/tasks/main.yml).
# Resolved values for whatever the main aws_terrafrom state currently has --
# if the floor infra is ever re-applied (new VPC, new subnet, etc.), re-run
# this role to pick up the new IDs before the next scale event.

region       = "{{ region }}"
cluster_name = "{{ cluster_name }}"

ami_id                   = "{{ ami_id }}"
worker_instance_type     = "{{ worker_instance_type }}"
worker_root_volume_size  = {{ worker_root_volume_size }}
ssh_key_name             = "{{ ssh_key_name }}"

private_subnet_id        = "{{ _tf.private_subnet_ids.value[0] }}"
worker_security_group_id = "{{ _tf.worker_security_group_id.value }}"
target_group_arn         = "{{ _tf.target_group_arn.value }}"
app_nodeport              = {{ app_nodeport }}
master_endpoint           = "{{ _tf.master_private_ip.value }}"
