---
- name: Fail if this role is included without tasks_from
  ansible.builtin.fail:
    msg: >-
      k8s-vault has no default tasks. Use
      '{ role: k8s-vault, tasks_from: vault_config.yml }' on localhost.
